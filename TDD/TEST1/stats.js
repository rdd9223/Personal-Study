exports.max = numbers => {
    Math.max(...numbers);
    // let result = numbers[0];
    // numbers.forEach(n => {
    //     if (n > result) {
    //         result = n;
    //     }
    // });
    // return result;
};
exports.min = numbers => Math.min(...numbers);